apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: my-replica
spec:
  replicas: 2
  selector:
    matchExpressions:
    - key: name 
      operator: In 
      values: [pod , test]
    - key: name 
      operator: In 
      values: [pod , test]
  template:
    metadata: 
      name: my-pod
      labels:
        name: pod
    spec:
      containers:
      - name: c00
        image: nginx